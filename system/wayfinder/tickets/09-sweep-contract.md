---
map: map.md
type: grilling
status: open
blocked-by:
  - 05-vault-folder-structure.md
---

## Question

**Sweep contract** — how phone inbox notes become vault entries. Decide: where the inbox note lives in the vault, the sweep algorithm (parse tags `#experiment`/`#paper`/`todo:` prefixes, append to monthly log with original timestamp, create Tasks, clear inbox), when it runs (agent session start + manual button), conflict handling if phone edited while laptop sweeps, and what the sweep report-back looks like ("5 notes → 4 log lines, 1 task").
