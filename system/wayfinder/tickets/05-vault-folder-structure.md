---
map: map.md
type: grilling
status: resolved
assignee: hp + agent (this session)

## Resolution

RESOLVED (provisional structure built during the pivot-build session) — tree created: `logs/ tasks/ milestones/ papers/pdf/ materials/ work-units/ targets/ meetings/{archive,print} writing/ publications/certificates/ money/ data/ scripts/ skills/ templates/ wayfinder/`. Filename conventions (IDs in titles) + nesting depth stay open for the Publication/Money just-in-time decisions. `README.md` in the vault root documents the map of folders + the location rule (vault = single source; agent dirs get copies/symlinks). `skills/` is decided-in.

## Question (original)

**Vault folder structure** — the exact directory tree and file-naming conventions for the whole system, now that the full object set is known. Folders for: logs, tasks, papers (+pdf), materials, work-units, targets, milestones, meetings, writing, publications, money, data, scripts, **skills** (decided: all skills live in the vault, git-versioned; agent-discovery locations get copies/symlinks at activation), templates. Decide: filename patterns (do object notes embed IDs like `wu-0042 — title.md`?), deep vs shallow nesting, and where the wayfinder/ directory sits.
